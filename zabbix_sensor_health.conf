# Sensor Health Report — drop this file into:
# C:\Program Files\Zabbix Agent\zabbix_agentd.d\

# Runs the full multi-site report and pushes all metrics via zabbix_sender
# Scheduled on the Zabbix SERVER side as a passive item every 10h
# Returns 1 = success, 0 = failed
UserParameter=sensor.health.run,python "C:\Program Files\Zabbix Agent\scripts\sensor_health_report.py" --send && echo 1 || echo 0
